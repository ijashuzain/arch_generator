name: arch_generator
description: A Dart package that automatically generates a clean and organized boilerplate code for defferent types architecture in Flutter projects.
version: 2.0.0
repository: https://github.com/ijashuzain/arch_generator

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  path: ^1.8.0
  meta: ^1.8.0
  args: ^2.4.1
  ansicolor: ^2.0.1


dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build: ^2.3.1
  build_runner: ^2.3.3
  build_test: ^2.1.7
  source_gen: ^1.3.0
